### Running the game
On MacOS or Linux, run
```$xslt
./gradlew rungame
```

On Windows, run
```$xslt
gradlew.bat rungame
```

### Running the server
On MacOS or Linux, run
```$xslt
./gradlew server
```

On Windows, run
```$xslt
gradlew.bat server
```

